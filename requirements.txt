# ============================================================
# 医学图像处理期末大作业 - 依赖清单
# 环境: conda env "medimg" (Python 3.10.20 @ D:\Anaconda_Envs\medimg)
# 安装: pip install -r requirements.txt
# 版本与已验证环境严格一致, 保证可复现
# ============================================================

# --- GUI ---
PySide6==6.11.2            # 界面框架 (仿 3D Slicer)

# --- 医学图像处理 ---
vtk==9.7.0                 # 三维重建 + 三维视图 (面绘制/体绘制/切平面)
pydicom==3.0.2             # DICOM 读取/解析
SimpleITK==2.5.6           # 图像处理 (增强/分割/重采样)
scikit-image==0.25.2       # 增强/分割算法库
scipy==1.15.3              # 科学计算 (分割算法依赖)
numpy==2.2.6               # 数组/体数据处理
opencv-python==5.0.0.93    # 补充图像处理
Pillow==12.3.0             # 图像格式支持
matplotlib==3.10.9         # 结果可视化/调试

# --- 打包 ---
pyinstaller==6.22.2        # 生成 exe
